<nav class="navbar navbar-default navbar-fixed">
    <div class="container-fluid" >
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navigation-example-2">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @if (Session["Role"].ToString().ToUpper().Contains("MANAGER"))
            {
                <a class="navbar-brand" href="#">MANAGER DASHBOARD</a>
            }
            @if (Session["Role"].ToString().ToUpper().Contains("TEAM LEAD"))
            {
                <a class="navbar-brand" href="#">TEAM LEAD DASHBOARD</a>
            }
            <span class="navbar-brand" >@ViewBag.Title</span>

        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-left"></ul>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        Account Settings
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li><a href="#">Another action</a></li>
                        <li><a href="#">Something</a></li>
                        <li class="divider"></li>
                        <li><a href="#">Separated link</a></li>
                    </ul>
                </li>
                @if (Session["Role"].ToString().ToUpper().Contains("MANAGER") || Session["Role"].ToString().ToUpper().Contains("TEAM LEAD"))
                {
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            @Session["Role"] Tools
                            <b class="caret"></b>
                        </a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Create Assessment", "CreateAssessment", "Assessment")</li>
                            <li>@Html.ActionLink("Create KPI", "CreateKPI", "KPI")</li>
                            <li>@Html.ActionLink("View Employees", "ViewEmployees", "Employee")</li>
                            <li><a href="#">Another action</a></li>
                            <li><a href="#">Something</a></li>
                            <li class="divider"></li>
                            <li><a href="#">Separated link</a></li>
                        </ul>
                    </li>
                }

                <li>
                    @Html.ActionLink("Log Out", "LogOut", "Login")
                </li>
            </ul>
        </div>
    </div>
</nav>
